// const { z } = require('zod');

// const deliveryChargeDTOSchema = z.object({
//            shop: z.string().min(1, "Vendor is required"),
//   language: z.enum(['zone', 'upazilla', 'district']).default('district'),
// });
// export default deliveryChargeDTOSchema;